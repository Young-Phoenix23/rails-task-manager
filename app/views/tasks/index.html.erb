<h1>My tasks!!!</h1>

<% @tasks.each do |task| %>
  <p><%= link_to task.title, task_path(task) %></p>
  <p><%= task.completed %></p>

  <p><%= link_to "Remove task", task_path(task), method: :delete %></p>

  <% end %>
<p><%= link_to "Add a new task", new_task_path %>
</p>
